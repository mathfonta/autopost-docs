schema: 1
story: '19.3'
story_title: 'Sugestão de melhor horário (GET /insights/best-posting-time)'
gate: PASS
status_reason: 'BUG-001 corrigido e verificado: comparação de segmento agora usa _normalize_segment (mesma função já usada em list_themes neste arquivo, case+acento-insensível) — confirmado por leitura direta do código (app/api/insights.py:186-202) e por teste novo (test_best_posting_time_exa_matches_despite_case_difference) que prova segmentos de grafia diferente casando corretamente. A cascata de 3 fontes agora funciona de fato: histórico → exa → fallback, todas verificadas. Implementação sólida com uma decisão de engenharia exemplar: o @dev descobriu que o AC4 original tinha premissa falsa (Exa não produzia horário) e, em vez de inventar dado, parou e levou a decisão ao usuário. Rodei os testes eu mesma (6/6) e a suíte completa (392/392).'
reviewer: 'Quinn (Test Architect)'
updated: '2026-07-25T01:00:00-03:00'

top_issues:
  - id: 'BUG-001'
    severity: low
    finding: 'RESOLVIDO — comparação de segmento entre `client.brand_profile.segment` e `WeeklyContext.segment` era exata (case-sensitive), fazendo a fonte Exa (AC4) provavelmente nunca ativar na prática. Corrigido com `_normalize_segment` (mesma função já usada em `list_themes`, case+acento-insensível) em `app/api/insights.py:186-202`. Verificado por leitura de código e por teste novo (`test_best_posting_time_exa_matches_despite_case_difference`) provando que "construção civil" e "Construção civil" agora casam.'
    suggested_action: 'Nenhuma — corrigido e verificado nesta sessão.'
    suggested_owner: dev

  - id: 'INFO-001'
    severity: low
    finding: 'AC5 originalmente previa usar copy_result.suggested_time "do próprio post" como parte do fallback — o @dev ajustou corretamente para não fazer isso neste endpoint genérico (sem contexto de post específico) e documentou a decisão. Concordo com o ajuste: fica reusado no frontend (Story 19.4), que já tem o post carregado.'
    suggested_action: 'Nenhuma — decisão correta, já documentada no Dev Agent Record.'
    suggested_owner: dev

waiver: { active: false }

quality_score: 96
expires: '2026-08-08T00:00:00-03:00'

evidence:
  tests_reviewed: 11
  risks_identified: 1
  trace:
    ac_covered: [1, 2, 3, 4, 5, 6, 7]
    ac_gaps: []

nfr_validation:
  security:
    status: PASS
    notes: 'Endpoint autenticado, sem exposição de dado de outro cliente — filtra por client_id.'
  performance:
    status: PASS
    notes: 'Query de histórico busca só publish_result do cliente autenticado; agregação em Python é aceitável no volume esperado (v1, conforme Dev Notes).'
  reliability:
    status: PASS
    notes: 'Com BUG-001 corrigido, a cascata de 3 fontes (histórico → exa → fallback) funciona de fato, não só em isolamento — confirmado por teste que cruza segmento de grafia diferente entre client e WeeklyContext.'
  maintainability:
    status: PASS
    notes: 'Reusa o padrão de cascata já estabelecido em /insights/strategy-recommendation (Story 18.2) — consistente com o resto do arquivo. Não copiou o bug pré-existente do /insights/weekly (business_segment inexistente) — usou brand_profile corretamente, boa atenção ao ler o código vizinho antes de escrever.'

recommendations:
  immediate: []
  future: []
