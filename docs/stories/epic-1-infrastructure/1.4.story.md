# Story 1.4 — Pipeline Celery de Agentes

**Status:** Done  
**Epic:** 1 — Infraestrutura Base  
**Story:** 1.4  
**Estimativa:** 3 pontos  
**Depende de:** 1.3

---

## Descrição

Como plataforma AutoPost, preciso de uma infraestrutura de filas Celery que orquestre o pipeline de agentes (Analista → Copywriter → Designer → aprovação → Publicador), com controle de estado persistido no banco, retries automáticos e rastreabilidade de cada etapa.

---

## Critérios de Aceitação

- [x] AC1: Modelo `ContentRequest` representa uma foto no pipeline com 9 estados (pending → published | failed)
- [x] AC2: Task `analyze_photo` transiciona pending → analyzing → copy, persiste `analysis_result`
- [x] AC3: Task `generate_copy` transiciona copy → design, persiste `copy_result` (caption, hashtags, cta, suggested_time)
- [x] AC4: Task `prepare_design` decide tipo visual (clean_photo vs card) baseado em `analysis_result.publish_clean`, transiciona → awaiting_approval
- [x] AC5: Task `publish_post` transiciona approved → publishing → published (chamada separada após aprovação)
- [x] AC6: Qualquer task em falha marca status = failed e registra `error_message`
- [x] AC7: Todas as tasks têm retry automático (max 2-3 tentativas com countdown)
- [x] AC8: `start_content_pipeline(request_id)` encadeia analyze → copy → design via Celery `chain`
- [x] AC9: Tabela `content_requests` criada no banco com índices em `client_id` e `status`

---

## Tasks

- [x] Criar `app/models/content_request.py` — modelo com enum `ContentStatus` (9 estados), campos de resultado JSONB
- [x] Criar `app/tasks/pipeline.py` — 4 tasks stub + `start_content_pipeline()` chain
- [x] Atualizar `app/tasks/__init__.py` — registrar `app.tasks.pipeline` no `include`
- [x] Atualizar `app/models/__init__.py` — exportar `ContentRequest` e `ContentStatus`
- [x] Gerar e aplicar migration `2287a06a1f54` — tabela `content_requests`
- [x] Escrever testes — 11 testes cobrindo todas as tasks e a chain

---

## Dev Notes

- Tasks Celery são síncronas — usar `asyncio.run()` (`_run_sync`) para chamar coroutines de DB
- Lógica de IA real implementada no Epic 2 — tasks desta story são stubs intencionais
- `publish_post` é chamado separadamente após aprovação manual do cliente (não faz parte da chain inicial)
- `source_channel` suporta: `app | whatsapp | google_drive | dropbox | onedrive`

---

## Escopo

**IN:** Infraestrutura de filas, modelo de estado, stubs das tasks  
**OUT:** Lógica de IA real (Epic 2), endpoints para disparar o pipeline (Epic 3), WebSocket de notificação (Epic 3)

---

## File List

- `app/models/content_request.py`
- `app/tasks/pipeline.py`
- `app/tasks/__init__.py` (modificado)
- `app/models/__init__.py` (modificado)
- `migrations/versions/2287a06a1f54_create_content_requests_table.py`
- `tests/test_pipeline.py`

---

## Change Log

| Data | Autor | O que mudou |
|------|-------|-------------|
| 2026-04-13 | @dev | Story implementada |
| 2026-04-13 | @sm | Story file criado retroativamente |
