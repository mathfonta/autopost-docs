# Story 2.3 — Agente Designer (Pillow)

**Status:** Done  
**Epic:** 2 — Agentes de IA  
**Story:** 2.3  
**Estimativa:** 3 pontos  
**Depende de:** 2.2

---

## Descrição

Como AutoPost, preciso que o Agente Designer processe a imagem conforme a decisão do Analista — foto de obra: adicionar logo discreto e salvar limpa; card clean: criar cartão com fundo sólido e texto — e fazer upload do resultado final para o Cloudflare R2.

---

## Critérios de Aceitação

- [x] AC1: Para `content_type=obra_realizada` e `publish_clean=True`: adiciona logo no canto inferior direito (transparência 60%), salva no R2
- [x] AC2: Para tipos card (`dica`, `promocao`): cria imagem 1080x1080px com fundo na cor primária do cliente, texto em destaque, logo
- [x] AC3: Para `content_type=antes_depois`: cria layout dividido (50%/50%) com labels "ANTES"/"DEPOIS" e logo
- [x] AC4: Imagem final salva no R2 com nova chave `processed/{request_id}/final.jpg`
- [x] AC5: `design_result` atualizado com `processed_photo_url` (URL pública do R2), `type`, `dimensions`
- [x] AC6: Imagem final ≤ 8MB (limite Meta Graph API) — MAX_FILE_SIZE = 8MB implementado
- [x] AC7: Testes unitários com imagens de fixture (não chama R2 real)

---

## Tasks

- [x] Criar `app/agents/designer.py` — função `process_image(request_id, analysis_result, brand_profile)`
- [x] Implementar 3 tipos: clean_photo, card, antes_depois via Pillow
- [x] Criar `app/core/storage.py` — cliente Cloudflare R2 (boto3) com upload/download/presigned_url
- [x] Atualizar `app/tasks/pipeline.py` — substituir stub de `prepare_design`
- [x] Criar `tests/test_designer_agent.py` — testes com imagens PIL em memória

---

## Dev Notes

- Pillow já está no requirements.txt
- boto3 compatível com R2 (endpoint customizado em `CLOUDFLARE_R2_ENDPOINT`)
- Logo do cliente vem de `brand_profile.logo_url` ou logo padrão AutoPost
- `brand_profile.primary_color` deve ser hex string (ex: `#1A3C6E`)
- Redimensionar imagem para 1080x1080 antes de processar (Instagram square)

---

## Escopo

**IN:** Processamento de imagem com Pillow, upload para R2  
**OUT:** Geração de imagens com DALL-E (v2.0), Stories/Reels (v2.0)

---

## File List

- `backend/app/agents/designer.py` — novo (agente principal, 211 linhas)
- `backend/app/core/storage.py` — novo (cliente R2 com boto3)
- `backend/app/tasks/pipeline.py` — atualizado (stub `prepare_design` substituído)
- `backend/tests/test_designer_agent.py` — novo (testes com PIL fixtures)

---

## QA Results

### Review Date: 2026-04-17

### Reviewed By: Quinn (Test Architect)

| Check | Status | Observação |
|-------|--------|------------|
| Code review | ⚠️ | Lógica sólida, mas _process_card usa fonte padrão PIL sem tamanho explícito |
| Unit tests | ✅ | 140/140 testes passando, fixtures PIL em memória |
| Acceptance criteria | ✅ | Todos os 7 ACs implementados: clean_photo, card, antes_depois, upload R2, ≤8MB |
| No regressions | ✅ | pipeline.py chama process_image corretamente |
| Performance | ✅ | Compression loop garante ≤8MB, _resize_square com LANCZOS |
| Security | ✅ | Sem secrets hardcoded, download externo com timeout=30s |
| Documentation | ✅ | Dev notes completas |

**Issues:**
- [severity: low] MNT-001: _process_card usa ImageDraw.text() com font=None (PIL default ~10px) — texto ilegível em cards 1080x1080. Recomendação: usar ImageFont com tamanho 40-60px.

### Gate Status

Gate: CONCERNS → docs/qa/gates/2.3-agente-designer.yml

---

## Change Log

| Data | Autor | O que mudou |
|------|-------|-------------|
| 2026-04-13 | @sm | Story criada |
| 2026-04-16 | @pm | Status Draft→InReview, ACs/tasks marcados, File List preenchida (regularização retroativa) |
| 2026-04-17 | @qa | QA Gate CONCERNS — 1 issue LOW (fonte PIL sem tamanho explícito em cards) |
