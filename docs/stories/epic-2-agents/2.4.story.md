# Story 2.4 — Agente Publicador (Meta Graph API)

**Status:** Done  
**Epic:** 2 — Agentes de IA  
**Story:** 2.4  
**Estimativa:** 5 pontos  
**Depende de:** 2.3

---

## Descrição

Como AutoPost, preciso que o Agente Publicador publique posts aprovados no Instagram Business e Página do Facebook via Meta Graph API, registre os IDs de publicação e inicie a coleta de métricas 24h depois.

---

## Critérios de Aceitação

- [x] AC1: Task `publish_post` publica imagem + legenda no Instagram Business via Graph API v19+
- [x] AC2: Se cliente tem Página do Facebook conectada, publica também no Facebook
- [x] AC3: `publish_result` salvo com: `instagram_post_id`, `facebook_post_id`, `permalink`, `published_at`
- [x] AC4: Status transiciona approved → publishing → published (ou failed com error_message)
- [x] AC5: Task agendada `collect_metrics` criada para rodar 24h após publicação (via Celery Beat)
- [x] AC6: Token Meta expirado → status = failed + `error_message` com orientação de renovação
- [x] AC7: Testes com mock da Meta Graph API (httpx mock)

---

## Tasks

- [x] Criar `app/agents/publisher.py` — funções `publish_to_instagram()` e `publish_to_facebook()`
- [x] Adicionar campos ao `Client`: `meta_access_token`, `instagram_business_id`, `facebook_page_id`
- [x] Criar migration para novos campos em `clients`
- [x] Criar task `collect_metrics` — coleta curtidas, comentários, alcance 24h depois
- [x] Atualizar `app/tasks/pipeline.py` — substituir stub de `publish_post`
- [x] Criar `tests/test_publisher_agent.py`

---

## Dev Notes

- Meta Graph API: `POST /{ig-user-id}/media` → `POST /{ig-user-id}/media_publish`
- Imagem deve estar em URL pública (usar presigned URL do R2 ou URL pública permanente)
- Token Meta expira a cada ~60 dias — adicionar verificação de expiração
- `META_ACCESS_TOKEN` no `.env` é o token de longa duração (Long-Lived Token)
- Métricas: endpoint `/{media-id}/insights?metric=impressions,reach,likes,comments`

---

## Escopo

**IN:** Publicação Instagram + Facebook, registro de IDs, coleta de métricas 24h  
**OUT:** Stories/Reels (v2.0), agendamento de posts (Epic 3), análise de desempenho (v1.1)

---

## File List

- `backend/app/agents/publisher.py` — novo (agente principal, 200 linhas)
- `backend/app/models/client.py` — atualizado (campos Meta: `meta_access_token`, `instagram_business_id`, `facebook_page_id`, `meta_token_expires_at`)
- `backend/app/core/meta_oauth.py` — novo (OAuth flow completo)
- `backend/app/api/meta.py` — novo (endpoints `/meta/connect`, `/meta/callback`, `/meta/status`)
- `backend/app/tasks/pipeline.py` — atualizado (stub `publish_post` substituído)
- `backend/migrations/versions/c4f1a8d92e3b_add_meta_fields_and_publish_result.py` — novo
- `backend/migrations/versions/d5e2f9a3c1b7_add_meta_oauth_fields_to_clients.py` — novo
- `backend/tests/test_publisher_agent.py` — novo
- `backend/tests/test_meta_oauth.py` — novo

---

## QA Results

### Review Date: 2026-04-17

### Reviewed By: Quinn (Test Architect)

| Check | Status | Observação |
|-------|--------|------------|
| Code review | ✅ | MetaAPIError com is_token_expired, fallback Facebook, 2-step Instagram correto |
| Unit tests | ✅ | 140/140 testes passando, mock httpx |
| Acceptance criteria | ✅ | Todos os 7 ACs: publicação IG+FB, publish_result, status transitions, token expirado |
| No regressions | ✅ | collect_metrics agendado via apply_async(countdown=86400) |
| Performance | ✅ | TIMEOUT=30s por chamada de API |
| Security | ✅ | access_token nunca logado, sem hardcode |
| Documentation | ✅ | Diretivas de segurança documentadas no docstring |

### Gate Status

Gate: PASS → docs/qa/gates/2.4-agente-publicador.yml

---

## Change Log

| Data | Autor | O que mudou |
|------|-------|-------------|
| 2026-04-13 | @sm | Story criada |
| 2026-04-16 | @pm | Status Draft→InReview, ACs/tasks marcados, File List preenchida (regularização retroativa) |
| 2026-04-17 | @qa | QA Gate PASS — 140/140 testes, todos os checks aprovados |
