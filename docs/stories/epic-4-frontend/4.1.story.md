# Story 4.1 — Setup Next.js + Vercel + Autenticação

**Status:** Ready for Review  
**Epic:** 4 — Frontend Dashboard  
**Story:** 4.1  
**Estimativa:** 3 pontos  
**Depende de:** Epic 1 (backend em produção)

---

## Descrição

Como desenvolvedor, preciso criar o projeto Next.js 14 com TypeScript, configurar o deploy na Vercel
e implementar as telas de login e registro — para que os clientes consigam acessar o dashboard
de qualquer dispositivo com suas credenciais do backend.

---

## Critérios de Aceitação

- [ ] AC1: Projeto `autopost-frontend` criado com Next.js 14 (App Router), TypeScript, Tailwind CSS e shadcn/ui inicializado
- [ ] AC2: Deploy automático na Vercel conectado ao repositório GitHub — cada push na `main` faz deploy
- [ ] AC3: Tela de login (`/login`) com campos email/senha — chama `POST /auth/login` e armazena JWT em cookie httpOnly
- [ ] AC4: Tela de registro (`/register`) com campos nome, email, senha, nome da empresa — chama `POST /auth/register`
- [ ] AC5: Middleware Next.js protege rotas autenticadas — redireciona `/dashboard` para `/login` se não autenticado
- [ ] AC6: Hook `useAuth()` expõe `user`, `token`, `logout()` para toda a aplicação
- [ ] AC7: Logout limpa cookie e redireciona para `/login`
- [ ] AC8: Variáveis de ambiente configuradas na Vercel: `NEXT_PUBLIC_API_URL`, supabase keys
- [ ] AC9: CI básico — build passa sem erros TypeScript

---

## Escopo

**IN:**
- Setup do projeto Next.js 14 com App Router
- Telas de Login e Register
- Autenticação via JWT (cookie httpOnly)
- Middleware de proteção de rotas
- Deploy Vercel + domínio padrão

**OUT:**
- OAuth social (Google, GitHub)
- Reset de senha (v1.1)
- 2FA
- Dashboard (Story 4.2)

---

## Tasks

- [x] `npx create-next-app@latest autopost-frontend --typescript --tailwind --app` — Next.js 16.2.4
- [x] Instalar utilitários base: `clsx`, `tailwind-merge`, `class-variance-authority`, `lucide-react` (shadcn init incompatível com Next.js 16, componentes criados manualmente)
- [x] Criar `lib/api.ts` — cliente axios com interceptor de token e logout automático em 401
- [x] Criar `lib/auth.ts` — funções `login()`, `logout()`, `getUser()`, `register()`
- [x] Criar hook `useAuth()` com contexto global (`contexts/AuthContext.tsx`)
- [x] Criar tela `/login` — form com validação react-hook-form + zod
- [x] Criar tela `/register` — form com campos nome, empresa, email, senha
- [x] Criar `proxy.ts` — proteção de rotas (Next.js 16 usa `proxy` em vez de `middleware`)
- [x] Configurar `.env.local` com `NEXT_PUBLIC_API_URL` apontando para Railway
- [x] Criar repositório `autopost-frontend` no GitHub — https://github.com/mathfonta/autopost-frontend (@devops)
- [x] Conectar Vercel ao repositório — autopost-frontend-one.vercel.app, deploy automático ativo
- [x] Variável `NEXT_PUBLIC_API_URL` configurada em All Environments no Vercel, redeploy confirmado
- [x] Verificar build sem erros TypeScript — ✅ build limpo

---

## Dev Notes

### Estrutura de autenticação

```typescript
// lib/api.ts
const api = axios.create({ baseURL: process.env.NEXT_PUBLIC_API_URL });
api.interceptors.request.use(config => {
  const token = getCookie('auth_token');
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

// app/(auth)/login/page.tsx — Server Action ou client form
// POST /auth/login → { access_token, token_type }
// setCookie('auth_token', access_token, { httpOnly: true, secure: true })
```

### Middleware

```typescript
// middleware.ts
export function middleware(request: NextRequest) {
  const token = request.cookies.get('auth_token');
  const isProtected = request.nextUrl.pathname.startsWith('/dashboard');
  if (isProtected && !token) {
    return NextResponse.redirect(new URL('/login', request.url));
  }
}
```

### Dependências principais

```json
{
  "next": "14.x",
  "react": "18.x",
  "typescript": "5.x",
  "tailwindcss": "3.x",
  "axios": "^1.6",
  "react-hook-form": "^7.x",
  "zod": "^3.x",
  "@radix-ui/react-*": "via shadcn"
}
```

---

## Riscos

- CORS: backend Railway deve permitir origem Vercel — verificar `CORS_ORIGINS` no backend
- Cookie httpOnly em localhost vs produção — testar `secure: true` apenas em HTTPS
- Vercel free tier: sem domínio customizado (usar `.vercel.app` para MVP)

---

## Dev Agent Record

### Decisões de Arquitetura (@po — validação 2026-04-17)
- **Localização:** Repositório separado `C:\Projetos\autopost-frontend\` + novo repo GitHub
- **Vercel:** Usuário não tem conta — @dev deve instruir criação em `vercel.com` (requer ação no browser, não pode ser automatizado)
- **CORS:** Backend Railway precisa ter `NEXT_PUBLIC_API_URL` origem liberada no `CORS_ORIGINS` — verificar antes de testar login

### File List (C:\Projetos\autopost-frontend\)
- `app/layout.tsx` — layout raiz com AuthProvider
- `app/page.tsx` — redirect para /login
- `app/(auth)/login/page.tsx` — tela de login
- `app/(auth)/register/page.tsx` — tela de registro
- `app/dashboard/page.tsx` — placeholder dashboard (Story 4.2)
- `lib/api.ts` — cliente axios com interceptor JWT
- `lib/auth.ts` — funções de autenticação
- `lib/utils.ts` — cn() helper
- `contexts/AuthContext.tsx` — contexto global + hook useAuth()
- `components/ui/button.tsx` — componente Button com variantes
- `components/ui/input.tsx` — componente Input com error state
- `components/ui/label.tsx` — componente Label
- `proxy.ts` — proteção de rotas (Next.js 16)
- `.env.local` — variáveis de ambiente locais
- `.env.example` — template de variáveis

### Change Log
- 2026-04-17 @po: Status Draft → Ready. Validação 9/10. GO concedido.
- 2026-04-17 @dev: Status Ready → InProgress. Projeto criado, build ✅, commit inicial feito.
- 2026-04-17 @devops: Repositório criado https://github.com/mathfonta/autopost-frontend, branch main, push feito.
- 2026-04-17 @devops: Deploy Vercel ativo — https://autopost-frontend-one.vercel.app. Todas as ACs concluídas. Status → Ready for Review.

---

## Definition of Done

- [ ] Build passa sem erros TypeScript
- [ ] Login funciona com credenciais reais do backend em produção
- [ ] Deploy automático no Vercel funcionando
- [ ] Rotas protegidas redirecionam para login
