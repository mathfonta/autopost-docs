# Story 22.4: Consumo a jusante — copywriter e theme_generator lêem os insights

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["typecheck", "pytest"]

## Story
**As a** copywriter e theme_generator,
**I want** ler o `scout_insights` do `brand_profile` quando ele existir,
**so that** o conteúdo gerado reflita o nicho real, os temas recorrentes e o estilo do cliente — e não apenas o segmento genérico declarado.

## Contexto / Motivação

Sem esta story, o Scout gravaria insights num campo que **nenhum agente lê** — o valor do epic depende dela. Hoje o copywriter lê apenas `brand_profile["segment"|"tone"|"city"|"company_name"]` (`app/agents/copywriter.py:475-478`) e o theme_generator seleciona temas por `segment` (`app/agents/theme_generator.py:76`). Esta story ensina ambos a dobrar o `scout_insights` (quando presente) nos seus prompts, mantendo fallback total ao segmento declarado.

## Acceptance Criteria

1. **AC1:** Quando `brand_profile["scout_insights"]` existe, o **copywriter** injeta no seu prompt os campos relevantes: `refined_niche`, `recurring_topics`, `visual_style`, `audience_notes` — enriquecendo (não substituindo) o contexto de segmento/tom já usado.
2. **AC2:** Quando `scout_insights` existe, `refined_niche` / `recurring_topics` enviesam tanto a **geração** quanto a **seleção** de temas em direção ao que o cliente realmente publica:
   - **Geração** (`app/agents/theme_generator.py`): injeta o contexto no prompt de estrutura de slides.
   - **Seleção** (adicionado — pre-flight): `app/api/insights.py:list_themes` reordena a lista de temas retornada, priorizando (dentro do mesmo grupo de `used_count`) os temas cujas `tags` têm maior overlap com `recurring_topics`. Quando `scout_insights` está ausente, a ordenação é **idêntica** à atual (só por `used_count`) — a pontuação de relevância é sempre 0 nesse caso, garantindo AC3 estruturalmente, não só por teste.
3. **AC3:** Quando `scout_insights` **não** existe (Scout não rodou, pulou ou falhou), o comportamento é **exatamente o de hoje** — nenhuma regressão no caminho sem Scout.
4. **AC4:** A leitura de `scout_insights` é defensiva: sub-objeto ausente, parcial ou com chaves faltando não quebra a geração (usar `.get` com defaults, como o padrão já existente no copywriter).
5. **AC5:** Testes: geração **com** `scout_insights` presente (prompt contém os insights); geração **sem** `scout_insights` (idêntico ao comportamento atual, sem regressão); `scout_insights` parcial/malformado (não quebra).

## Dev Notes

- **Copywriter** — pontos de leitura do brand_profile: `app/agents/copywriter.py:475-478`; a montagem do prompt com contexto do cliente está por volta de `copywriter.py:656-659` (`Empresa/Segmento/Tom/Cidade`). Adicionar uma seção condicional de insights ali.
- **theme_generator** — `app/agents/theme_generator.py:49-85`: hoje localiza o tema na `THEME_LIBRARY` por segmento e injeta `company_name`/`segment` no `SLIDE_STRUCTURE_PROMPT`. Estender para considerar `recurring_topics`/`refined_niche` quando presentes.
- **Contrato de entrada** = o `scout_insights` produzido na Story 22.2 e persistido na 22.3. Esta story só **lê** — não altera o Scout nem a persistência.
- Manter o estilo defensivo com `.get(chave, default)` que já é padrão nesses agentes — o `scout_insights` é sempre opcional.
- Não tocar no pipeline de foto/vídeo além da leitura adicional do brand_profile já disponível.

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-07-22 | 1.0 | Story criada a partir do Epic 22 | @sm (River) |
| 2026-07-22 | 1.1 | Validação 10 pontos: 8/10 GO. Status Draft → Ready | @po (Pax) |
| 2026-07-23 | 1.2 | Pre-flight (@dev): AC2 expandido para incluir reordenação em app/api/insights.py:list_themes (seleção, não só geração). Aprovado pelo usuário em tempo real. Confidence baixo não gateia injeção (decisão: sempre injetar se presente). | @po (Pax) |
| 2026-07-23 | 1.3 | Implementação completa (modo pre-flight). Todos os AC1-AC5 atendidos (incluindo o AC2 expandido). 16 testes novos, 351/351 na suíte completa (regressão zero). Status Ready for Review → aguardando @qa. | @dev (Dex) |

## Dev Agent Record

### Agent Model Used
claude-sonnet-5 (@dev — Dex), pre-flight planejado e aprovado por claude-opus-4-8 (@aios-master — Orion) na fase de epic/stories

### Debug Log References
- `python -m pytest tests/test_scout_insights_consumption.py -v` → 16/16 passed
- `python -m pytest tests/test_themes.py -v` → 9/9 passed (verificação dedicada, já que insights.py foi tocado)
- `python -m pytest -q` (suíte completa do backend) → 351 passed (335 pré-existentes + 16 novos), 0 failed
- `python -m py_compile app/agents/copywriter.py app/agents/theme_generator.py app/api/insights.py` → sintaxe válida
- CodeRabbit: segue indisponível neste WSL (confirmado 4x nesta sessão) — não re-verificado

### Completion Notes List
- **Decisão de pre-flight — escopo expandido para "seleção" (AC2):** os Dev Notes originais só apontavam para `theme_generator.py` (geração de conteúdo dos slides). Durante o pre-flight, identifiquei que `app/api/insights.py:list_themes` é o candidato mais literal para "seleção de temas" — é o endpoint que decide quais temas o usuário vê no picker. Usuário aprovou expandir o escopo para os dois arquivos.
- **Decisão de pre-flight — sem piso de confidence:** `scout_insights` é injetado sempre que presente, independente do valor de `confidence`. Nenhuma AC pedia esse gate; a 22.2 já aplica um teto conservador no próprio valor. Registrado como sugestão futura, não implementado agora.
- **Design da reordenação em `list_themes` — garante AC3 estruturalmente:** `_theme_relevance_score()` retorna 0 sempre que `recurring_topics` está ausente/vazio/malformado — então a chave de ordenação `(used_count, -relevance_score)` colapsa exatamente na ordenação atual (`used_count` puro) quando o Scout não rodou. Não é só testado, é garantido pela própria fórmula.
- **Todos os três pontos de injeção são estritamente aditivos e defensivos:** `scout_insights` ausente, `{}`, `None`, string solta, ou com `recurring_topics` no tipo errado — nenhum desses cenários quebra `generate_copy_with_ai`, `generate_slide_structure` ou `_theme_relevance_score`. Testado com `@pytest.mark.parametrize` cobrindo 4-5 variações de malformação por função.
- **Nenhuma mudança no pipeline de foto/vídeo além da leitura do brand_profile já disponível** — conforme Dev Notes da story.
- **Import/variável pré-existentes não tocados:** `timedelta` não usado em `meta.py` (Story 22.3) e uma variável `monday` não usada em `insights.py` (linha 60, fora da área desta story) — ambos pré-existentes, fora de escopo, não corrigidos.
- **Reuso de `_normalize_segment`** (já existente em `insights.py`) para normalizar `recurring_topics` antes de comparar com as `tags` dos temas — evita duplicar lógica de normalização de texto.

### File List
- `backend/app/agents/copywriter.py` (modificado) — injeta `scout_section` no prompt (AC1)
- `backend/app/agents/theme_generator.py` (modificado) — injeta `scout_context` no `SLIDE_STRUCTURE_PROMPT` (AC2, geração)
- `backend/app/api/insights.py` (modificado) — `_theme_relevance_score()` + reordenação em `list_themes` (AC2, seleção — escopo expandido no pre-flight)
- `backend/tests/test_scout_insights_consumption.py` (criado) — 16 testes cobrindo os três pontos de injeção
- `docs/stories/epic-22-agente-scout/22.4.story.md` (modificado) — AC2 emendado no pre-flight, Dev Agent Record preenchido

## QA Results
_A preencher pelo @qa_
