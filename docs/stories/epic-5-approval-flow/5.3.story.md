# Story 5.3 — Menu de Intenção antes do Upload

**Status:** Ready for Review  
**Epic:** 5 — Fluxo de Aprovação Avançado  
**Story:** 5.3  
**Estimativa:** 3 pontos  
**Depende de:** Story 4.2 (PhotoUploadFAB existente)

---

## Descrição

Como cliente AutoPost, quero escolher o tipo de conteúdo que quero criar antes de enviar a foto — para que o agente gere uma legenda adequada ao formato escolhido, sem eu precisar editar depois.

---

## Critérios de Aceitação

- [ ] AC1: Ao clicar no FAB "Nova Foto", antes de abrir o file picker, exibe um bottom sheet / modal com as opções de intenção
- [ ] AC2: Opções disponíveis no MVP (ícone + rótulo):
  - 📸 Post Simples — publicação de obra ou produto
  - 🔨 Obra em Andamento — progresso de projeto
  - ✅ Obra Concluída — resultado final
  - 💬 Engajamento — pergunta ou enquete para a audiência
  - 🎉 Bastidores — momento da equipe ou processo
- [ ] AC3: Usuário seleciona uma opção → file picker abre normalmente
- [ ] AC4: Opção selecionada é enviada no campo `content_type` do `POST /content-requests`
- [ ] AC5: O Agente Copywriter recebe `content_type` no contexto e adapta o tom e estrutura da legenda
- [ ] AC6: Na prévia, o tipo de conteúdo selecionado é exibido como badge discreto ("Obra Concluída")
- [ ] AC7: Se usuário fechar o bottom sheet sem selecionar, nada acontece (fluxo cancelado)

---

## Escopo

**IN:**
- Bottom sheet de seleção de intenção no mobile, modal no desktop
- 5 tipos de conteúdo para o MVP
- `content_type` passado ao backend e usado pelo Copywriter
- Badge do tipo na tela de aprovação

**OUT:**
- Carrossel e Antes/Depois (requer múltiplas fotos — Story futura)
- Reels/Roteiro (v2.0)
- Criação de tipos customizados pelo usuário
- Seleção de intenção na tela de Histórico

---

## Tasks

### Backend
- [ ] Verificar se campo `content_type` já existe em `ContentRequest` — se não, adicionar migration
- [ ] Atualizar schema `ContentRequestCreate` para aceitar `content_type: str | None`
- [ ] Atualizar `generate_copy` task para injetar `content_type` no system prompt do Copywriter
  - Ex: "Tipo de conteúdo: Obra Concluída. Tom: celebração e resultado."
- [ ] Adicionar mapeamento de `content_type → instrução de prompt` em `app/agents/copywriter.py`
- [ ] Teste unitário: diferentes `content_types` geram instruções de prompt diferentes

### Frontend
- [ ] Criar componente `IntentMenu.tsx` em `frontend/components/dashboard/` — bottom sheet mobile / modal desktop
  - 5 opções com ícone + rótulo
  - Estado: fechado → selecionando → selecionado
- [ ] Modificar `PhotoUploadFAB.tsx` para abrir `IntentMenu` antes do file picker
- [ ] Passar `content_type` selecionado no `POST /content-requests` via `uploadContentRequest(file, contentType)`
- [ ] Atualizar `frontend/lib/api.ts` — adicionar `content_type` ao payload
- [ ] Adicionar badge `ContentTypeBadge` no `PostCard.tsx` para exibir o tipo escolhido

---

## Dev Notes

**Tipos de conteúdo — mapeamento de prompt:**
```python
CONTENT_TYPE_PROMPTS = {
    "post_simples": "Post direto apresentando o trabalho. Tom: profissional e claro.",
    "obra_andamento": "Obra em progresso. Tom: transparência e confiança no processo.",
    "obra_concluida": "Resultado final entregue. Tom: celebração, orgulho e resultado.",
    "engajamento": "Post para gerar interação. Tom: pergunta direta, convite à participação.",
    "bastidores": "Momento humano da equipe. Tom: autêntico, próximo, humanizado.",
}
```

**Componente IntentMenu:**
```tsx
// Props: onSelect(contentType: string) | onClose()
// Mobile: bottom sheet com overlay
// Desktop: modal centralizado pequeno
// Cada opção: ícone emoji + rótulo + descrição curta
```

---

## File List

**Backend:**
- `backend/app/models/content_request.py` — campo `content_type`
- `backend/migrations/versions/c3d4e5f6a1b2_add_content_type_to_content_requests.py` — migration
- `backend/app/schemas/content.py` — `content_type` no response
- `backend/app/api/content.py` — Form param `content_type` no `submit_photo`
- `backend/app/agents/copywriter.py` — `CONTENT_TYPE_PROMPTS`, param `user_content_type`
- `backend/app/tasks/pipeline.py` — passa `content_type` ao copywriter
- `backend/tests/test_copywriter_agent.py` — 2 novos testes de intenção
- `backend/tests/test_content_api.py` — teste de upload com `content_type`

**Frontend:**
- `frontend/lib/types.ts` — campo `content_type`
- `frontend/components/dashboard/IntentMenu.tsx` — NOVO: menu de intenção
- `frontend/components/dashboard/PhotoUploadFAB.tsx` — integra IntentMenu antes do file picker
- `frontend/components/dashboard/PostCard.tsx` — badge de tipo de conteúdo
- `frontend/app/posts/[id]/page.tsx` — prop `retryCount` em ApprovalButtons

---

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-04-23 | @sm (River) | Story criada — Draft |
| 2026-04-23 | @po (Pax) | Validação GO — 7.5/10 — Status: Ready |
| 2026-04-23 | @dev (Dex) | Implementação completa — Status: Ready for Review |
