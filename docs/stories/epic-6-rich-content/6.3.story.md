# Story 6.3 — Frontend Multi-Foto

**Status:** Ready for Review  
**Epic:** 6 — Conteúdo Rico: Múltiplas Fotos, Antes/Depois e Carrossel  
**Story:** 6.3  
**Estimativa:** 3 pontos  
**Depende de:** Story 6.1 (endpoint aceita múltiplas fotos)  
**Pode ser paralela a:** Story 6.2 (pipeline é independente do frontend)

---

## Descrição

Como cliente AutoPost, quero selecionar múltiplas fotos na tela principal e ver uma prévia de todas antes de enviar — para que eu possa verificar a seleção, remover fotos indesejadas e escolher o tipo de conteúdo adequado (antes/depois ou carrossel) sem precisar reiniciar o fluxo.

---

## Critérios de Aceitação

- [ ] **AC1 — FAB aceita múltipla seleção:** O botão de câmera (FAB) suporta seleção de 1–10 fotos. No mobile, o bottom sheet exibe "Escolher da galeria (múltiplas)" como opção. No desktop, o file picker abre com `multiple` habilitado.
- [ ] **AC2 — Preview antes do envio:** Após selecionar 2+ fotos, o usuário vê uma fila de thumbnails (imagens 60×60px) em linha horizontal com um "×" para remover cada foto individualmente — antes de confirmar o upload.
- [ ] **AC3 — Upload multi-foto via FormData:** `POST /content-requests` é chamado com `FormData` contendo N entradas `photos` (uma por arquivo). O campo legado `photo` não é usado na nova implementação.
- [ ] **AC4 — Menu de Intenção exibe novos tipos:** Quando 2+ fotos são selecionadas, o `IntentMenu` exibe as opções `Antes/Depois` e `Carrossel` além das existentes. Com 1 foto, apenas os tipos simples são exibidos (comportamento atual).
- [ ] **AC5 — `content_type` enviado no FormData:** O valor selecionado no `IntentMenu` é incluído como campo `content_type` no FormData.
- [ ] **AC6 — `PostCard` exibe thumbnails múltiplos:** Quando `photo_urls` tem 2+ itens, o card exibe uma faixa de thumbnails (máx 3 visíveis + indicador "+N") ao invés da foto única. O clique abre o carrossel de preview em tela cheia (modal simples com setas).
- [ ] **AC7 — Retrocompat visual:** Cards com foto única continuam exibindo a foto como antes (`photo_url`). Nenhuma regressão visual nos cards existentes.
- [ ] **AC8 — Validação no cliente:** Antes de enviar, validar: máx 10 fotos (erro inline), `before_after` = exatamente 2 (aviso antes de enviar), `carousel` = 2–10 (aviso antes de enviar).
- [ ] **AC9 — Build limpo:** `npm run build` e `npm run typecheck` passam sem erros. Todos os tipos TypeScript corretos.
- [ ] **AC10 — Spinner e error handling:** Durante upload multi-foto, spinner é exibido por foto (ex: "Enviando 2/3..."). Em caso de erro, mensagem clara sem dados residuais no estado.

---

## Escopo

**IN:**
- `PhotoUploadFAB` / `PhotoUploadButton` com suporte a múltipla seleção
- Preview de fotos selecionadas antes do envio (thumbnails + botão remover)
- `POST /content-requests` com `FormData` multi-foto
- `IntentMenu` com opções `Antes/Depois` e `Carrossel` quando 2+ fotos
- `PostCard` com display de `photo_urls` múltiplas (thumbnails + indicador)
- Tipos TypeScript para `photo_urls`

**OUT:**
- Modal de preview de carrossel full-screen com animações avançadas
- Edição ou reordenação de fotos após upload
- Progresso de upload por foto via WebSocket (futura Story 7.x)
- Testes de integração E2E (testar no staging é suficiente para esta story)

---

## Tasks

### Upload Multi-foto
- [ ] Localizar componente de upload (FAB/PhotoUploadButton) e adicionar `multiple` no `<input type="file">`
- [ ] No bottom sheet mobile, adicionar opção "Galeria (múltiplas fotos)" com `multiple` no input
- [ ] Criar estado `selectedFiles: File[]` para gerenciar as fotos selecionadas antes do envio

### Preview antes do envio
- [ ] Criar componente `PhotoPreviewQueue` em `frontend/components/dashboard/PhotoPreviewQueue.tsx`
  - Exibe thumbnails em `<img>` de 60×60px com `URL.createObjectURL(file)` 
  - Botão "×" remove foto da lista
  - Badge com contagem `{n} foto(s) selecionada(s)`
- [ ] Integrar `PhotoPreviewQueue` acima do `IntentMenu` no fluxo de upload

### IntentMenu com novos tipos
- [ ] Atualizar `IntentMenu` para receber prop `photoCount: number`
- [ ] Quando `photoCount >= 2`: exibir opções `Antes/Depois` (`before_after`) e `Carrossel` (`carousel`) no topo da lista
- [ ] Quando `photoCount === 1`: exibir apenas tipos simples (comportamento atual)
- [ ] Adicionar validação: se `before_after` selecionado e `photoCount !== 2`, exibir aviso inline

### Upload via FormData
- [ ] Atualizar função `submitContentRequest` (ou equivalente) em `frontend/lib/api.ts`:
  - Aceitar `files: File[]` no lugar de `file: File`
  - Construir `FormData` com múltiplas entradas `photos` (uma por arquivo)
  - Incluir `content_type` se selecionado
- [ ] Atualizar tipo TypeScript: `ContentRequestCreate` com `files: File[]`
- [ ] Atualizar spinner para mostrar "Enviando {n} fotos..."

### PostCard com multi-foto
- [ ] Atualizar tipo `ContentRequest` em `frontend/lib/types.ts`: adicionar `photo_urls?: string[]`
- [ ] Em `PostCard.tsx` (ou `PostCardMedia.tsx`): se `photo_urls?.length > 1`, exibir faixa de thumbnails
  - Mostrar as 3 primeiras fotos em thumbnails 48×48px lado a lado
  - Se `length > 3`: exibir `+{n}` no último placeholder
  - Clicar abre modal simples com navegação (seta ← →)
- [ ] Se `photo_urls` ausente ou length === 1: exibir `photo_url` como antes (retrocompat)

### Tipos TypeScript
- [ ] `frontend/lib/types.ts`: `photo_urls?: string[] | null` em `ContentRequest`
- [ ] `frontend/lib/api.ts`: atualizar `submitContentRequest` para aceitar `File[]`

---

## Dev Notes

### Input Multi-foto (HTML)

```tsx
// Desktop
<input
  type="file"
  accept="image/jpeg,image/png,image/webp"
  multiple        // ← adicionar
  onChange={handleFileChange}
/>

// Bottom sheet mobile — adicionar nova opção
<button onClick={() => galleryInputRef.current?.click()}>
  Escolher da galeria (múltiplas)
</button>
<input ref={galleryInputRef} type="file" multiple accept="image/*" ... />
```

### FormData Multi-foto

```typescript
async function submitPhotos(files: File[], contentType?: string) {
  const formData = new FormData();
  files.forEach(file => formData.append("photos", file));
  if (contentType) formData.append("content_type", contentType);
  
  const res = await apiClient.post("/content-requests", formData);
  return res.data;
}
```

**Atenção:** Não definir `Content-Type` header manualmente — o browser define automaticamente com boundary para multipart.

### PhotoPreviewQueue — componente simples

```tsx
export function PhotoPreviewQueue({ files, onRemove }: {
  files: File[];
  onRemove: (index: number) => void;
}) {
  return (
    <div className="flex gap-2 flex-wrap my-2">
      {files.map((file, i) => (
        <div key={i} className="relative">
          <img
            src={URL.createObjectURL(file)}
            className="w-16 h-16 object-cover rounded"
          />
          <button
            onClick={() => onRemove(i)}
            className="absolute -top-1 -right-1 bg-red-500 text-white rounded-full w-4 h-4 text-xs flex items-center justify-center"
          >×</button>
        </div>
      ))}
      <span className="text-sm text-gray-500 self-center">
        {files.length} foto(s)
      </span>
    </div>
  );
}
```

**Atenção:** Revogar URLs após uso: `useEffect(() => () => files.forEach(f => URL.revokeObjectURL(f.name)), [files])` — na prática usar `URL.createObjectURL(file)` inline com cleanup adequado.

### IntentMenu — prop photoCount

```tsx
// Novos tipos disponíveis apenas com 2+ fotos
const options = [
  ...(photoCount >= 2 ? [
    { value: "before_after", label: "Antes/Depois", icon: "🔄" },
    { value: "carousel", label: "Carrossel", icon: "📸" },
  ] : []),
  { value: "post_simples", label: "Post Simples", icon: "📷" },
  // ... outros tipos existentes
];
```

### PostCard — lógica de exibição

```tsx
// No PostCard / PostCardMedia:
const hasMultiplePhotos = (item.photo_urls?.length ?? 0) > 1;

if (hasMultiplePhotos) {
  // Exibir faixa de thumbnails
  return <MultiPhotoStrip urls={item.photo_urls!} />;
} else {
  // Exibir foto única (retrocompat)
  return <img src={item.photo_url} ... />;
}
```

### Integração com Story 6.2

O frontend não precisa saber se o pipeline está processando multi-foto. O polling existente de `status` em `useContentRequests` funciona sem mudanças. Quando o status chega a `awaiting_approval`, o card exibe os resultados disponíveis.

---

## File List

> Lista original (planejada por @sm/@po no draft) abaixo, seguida da lista REAL do que foi tocado nesta implementação — ver Dev Agent Record para o porquê da divergência.

**Planejado originalmente (não usado como guia — ver Dev Agent Record):**
- ~~`frontend/lib/types.ts`~~, ~~`frontend/lib/api.ts`~~, ~~`PhotoPreviewQueue.tsx`~~, ~~`PhotoUploadFAB.tsx`~~, ~~`IntentMenu.tsx` (prop `photoCount`)~~ — já implementados antes desta story, por um fluxo diferente do descrito aqui (ver Dev Agent Record)

**Real (esta implementação, 2026-07-25):**
- `frontend/components/dashboard/PostCard.tsx` (modificado) — `MultiPhotoStrip` (thumbnails + indicador `+N`) e `PhotoCarouselModal` (tela cheia com navegação ← →) adicionados; único arquivo tocado

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-5 (@dev — Dex)

### Completion Notes List

**Divergência de arquitetura descoberta antes de implementar:** ao investigar a story, constatei que o upload multi-foto (seleção, preview com miniaturas removíveis, envio via FormData com múltiplas entradas `photos`, validação client-side de 2–10 fotos) **já estava implementado em produção**, mas por um caminho diferente do que esta story descrevia:
- A story previa um fluxo `IntentMenu` (escolher tipo de conteúdo DEPOIS de selecionar fotos) com componentes novos (`PhotoPreviewQueue`, prop `photoCount`).
- O que o time construiu de fato: fluxo "tipo de post primeiro" (`lib/post-types.tsx` → usuário escolhe "Carrossel" ANTES de enviar fotos), com a lógica multi-foto dentro de `UploadScreen.tsx` (modo `multi-image`) e o envio em `app/dashboard/page.tsx` (`uploadFile`).
- Isso cobre os ACs 1, 2, 3, 5, 7, 8 (seleção múltipla, preview com remover, FormData com `photos`, `content_type` enviado, retrocompat, validação 2–10) — só não da forma como a story descrevia tecnicamente, mas o comportamento visível ao usuário é equivalente.
- **AC4 (Antes/Depois como tipo separado) não existe** — `lib/post-types.tsx` só tem `feed_photo`, `carousel`, `reels`, `story`. Fora de escopo por decisão explícita do usuário nesta sessão (2026-07-25): implementar somente o gap real, sem criar Antes/Depois.
- **AC6 (PostCard exibindo múltiplas fotos) era o único gap real** — antes desta implementação, `PostCard.tsx` sempre mostrava uma foto só (`post.design_result?.processed_photo_url ?? post.photo_url`), mesmo quando `post.photo_urls` tinha várias.

**O que foi implementado agora:**
- `hasMultiplePhotos = (post.photo_urls?.length ?? 0) > 1` como condicional explícito (AC7 — retrocompat: post de foto única continua exatamente como antes, sem qualquer mudança de comportamento).
- `MultiPhotoStrip`: grid 3 colunas com as 3 primeiras fotos de `post.photo_urls`; se houver mais, overlay `+N` na última visível.
- Clique na área de imagem de um post multi-foto abre `PhotoCarouselModal` (tela cheia, `fixed inset-0 z-50`, navegação ← → com wraparound, contador "N / total", fecha no X ou clique fora). Para posts de foto única, o clique continua indo para `onOpen`/detalhe do post como antes — só intercept quando `hasMultiplePhotos`.
- `AC9`: `npm run typecheck` e `npm run build` (Next.js/Turbopack) limpos, 13/13 páginas geradas.
- `AC10` (spinner "Enviando N fotos") não se aplica a este escopo — já existe no `UploadScreen.tsx` (implementação pré-existente, `pendingFiles.length/10 fotos`), fora do arquivo tocado nesta story.

**Não testado em navegador real** — validado via `tsc --noEmit` e `next build`. Recomendo checar em produção com um post de carrossel real (2+ fotos) antes do próximo beta test com usuário externo.

### File List
Ver seção "File List" acima (única fonte, não duplicada aqui).

---

## Riscos

| Risco | Probabilidade | Mitigação |
|-------|--------------|-----------|
| `URL.createObjectURL` leak de memória | Baixa | Revogar URLs no cleanup do useEffect |
| Regressão visual em PostCard (foto única) | Média | Condicional explícito em `hasMultiplePhotos`; testar em staging |
| FormData com `photos` não aceito pelo backend (campo name) | Baixa | Verificar AC3 com curl antes de implementar frontend |
| TypeScript strict com `File[]` vs `File` | Baixa | Atualizar todos os tipos e resolver warnings antes de build |

---

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-04-24 | @sm (River) | Story criada — Draft — YOLO mode |
| 2026-04-24 | @po (Pax) | Validação GO — 9/10 — Nota técnica: revokeObjectURL em Dev Notes usa f.name incorretamente (não bloqueia) — Status: Ready |
| 2026-07-25 | @dev (Dex) | Descoberta: upload multi-foto já implementado em produção por arquitetura diferente da prevista (ver Dev Agent Record). Escopo reduzido por decisão do usuário: só fechar o gap real (PostCard exibindo múltiplas fotos, AC6). Antes/Depois (AC4) fica fora de escopo. Implementado `MultiPhotoStrip` + `PhotoCarouselModal` em `PostCard.tsx`. typecheck + build limpos. Status Ready → Ready for Review. |
| 2026-07-25 | @qa (Quinn) | Gate CONCERNS — código revisado por leitura independente, correto e bem construído; único item aberto é checagem visual em navegador real antes do beta test. Ver QA Results. |

---

## QA Results

### Review Date: 2026-07-25

### Reviewed By: Quinn (Test Architect)

### Escopo desta revisão
Apenas o gap fechado nesta sessão (AC6 — PostCard multi-foto). Os ACs 1/2/3/5/7/8 referem-se a funcionalidade pré-existente (implementada antes desta story, por arquitetura diferente da descrita no documento original) — não foram re-verificados com o mesmo rigor de uma feature nova, mas sua presença no código foi confirmada durante a investigação desta sessão (`UploadScreen.tsx` modo `multi-image`, `app/dashboard/page.tsx` função `uploadFile`, `lib/post-types.tsx`). AC4 (Antes/Depois) está fora de escopo por decisão explícita do usuário.

### Verificação de código (leitura direta, não só o relato do @dev)

- **AC6** — confirmado em `PostCard.tsx`: `hasMultiplePhotos = (post.photo_urls?.length ?? 0) > 1` (linha 91); `MultiPhotoStrip` renderiza grid 3 colunas com overlay `+N` na última thumbnail visível quando há mais de 3 fotos; clique abre `PhotoCarouselModal` com navegação ← → (wraparound), contador de posição, fecha por X ou clique fora.
- **AC7** — confirmado: quando `hasMultiplePhotos` é falso, o card renderiza exatamente o `<img>` único de antes, sem nenhuma mudança de comportamento (retrocompat real, não só alegada).
- **AC9** — confirmado: rodei eu mesma `npm run typecheck` e `npm run build` sobre o diff — ambos limpos, 13/13 páginas geradas.

### Investigação adicional (achado de QA, não do @dev)

Ao revisar a mudança no clique da área de imagem (que agora abre o carrossel em vez de `onOpen`/navegação para posts multi-foto), investiguei se isso quebraria alguma navegação existente. Descobri que `PostCard` é usado em **exatamente 1 lugar** em todo o app (`app/dashboard/page.tsx:513`), sempre com `onOpen` (só para posts `awaiting_approval`). Como a linha "Toque para revisar" abaixo do card também chama `onOpen`, não há perda de funcionalidade no único caminho real de uso — os branches de `isFailed`/`isRejected`/`published` no componente são código não-alcançável hoje (condição pré-existente, não introduzida por esta story). Documentado como INFO-001 no gate, não bloqueante.

### Segurança
Sem `dangerouslySetInnerHTML`; URLs de foto são as mesmas já usadas em outros pontos do app (R2 presigned) — nenhuma nova superfície de risco.

### Pendência

**MANUAL-001** (severidade baixa): o novo código nunca foi visto rodando em navegador real — só validado por `tsc`/`build`. Risco é baixo (é lógica de renderização client-side pura, categoria diferente da que já causou problema neste projeto — ver Story 22.5, que envolvia OAuth/redirect). Recomendado conferir visualmente antes do beta test da semana que vem; não bloqueante.

### Gate Status

Gate: **CONCERNS** → `docs/qa/gates/6.3-postcard-multi-foto.yml`

Motivo: implementação correta por leitura de código e validações automatizadas; único item aberto é checagem visual em navegador real, recomendada antes do beta test mas não bloqueante.
