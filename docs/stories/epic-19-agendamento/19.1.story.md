# Story 19.1: Modelo de agendamento — `scheduled_for`, status `scheduled` e endpoint de agendar

## Status
Done

## Executor Assignment
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["pytest"]
estimate: "Médio (~3 pontos)"
collaborators: "@data-engineer (migração Alembic + índice)"

## Story
**As a** cliente que acabou de aprovar um post,
**I want** poder marcar esse post para ser publicado num horário futuro em vez de agora,
**so that** meu conteúdo saia no melhor horário mesmo que eu o tenha montado fora dele.

## Contexto / Motivação
Hoje o único caminho após `awaiting_approval` é `POST /content-requests/{id}/approve` (`app/api/content.py:420`), que seta `approved` e dispara `publish_post.delay()` imediatamente. Não existe estado de "agendado". Esta story cria a base de dados e a API de agendamento; a publicação automática no horário é a Story 19.2.

## Acceptance Criteria

1. **AC1 — Novo status:** O enum `ContentStatus` (`app/models/content_request.py:23`) ganha o valor `scheduled = "scheduled"`, posicionado conceitualmente entre `approved` e `publishing` no ciclo de vida.
2. **AC2 — Novo campo:** `ContentRequest` ganha `scheduled_for: Mapped[datetime | None]` **timezone-aware** (`DateTime(timezone=True)`), default `NULL`.
3. **AC3 — Migração:** Migração Alembic cria a coluna `scheduled_for` e adiciona o valor `scheduled` ao tipo enum, além do índice composto `(status, scheduled_for)` (necessário para o Beat da Story 19.2). Migração aplica e reverte sem erro.
4. **AC4 — Endpoint de agendar:** Novo `POST /content-requests/{id}/schedule` com body `{ "scheduled_for": "<ISO8601 com timezone>" }`. Só aceita posts em `awaiting_approval`. Seta `status=scheduled` + `scheduled_for` e **NÃO** chama `publish_post`. Retorna `{ id, status: "scheduled", scheduled_for }`.
5. **AC5 — Validações:** Rejeita (HTTP 422/400) `scheduled_for` no passado ou a mais de 30 dias no futuro. Rejeita (HTTP 409) se o status atual não for `awaiting_approval`. Rejeita (HTTP 403) se o post não pertence ao cliente autenticado (mesmo padrão do `/approve`).
6. **AC6 — Isolamento do caminho atual:** `POST /{id}/approve` permanece **byte-idêntico** — nenhuma mudança no fluxo "Publicar agora".
7. **AC7 — Testes:** Cobrem agendar com sucesso, `scheduled_for` no passado, > 30 dias, status errado, post de outro cliente, e a não-invocação de `publish_post` ao agendar.

## Dev Notes
- **Decisão de arquitetura (Aria):** endpoint **separado** `/schedule`, não parâmetro no `/approve` — zero risco de regressão no caminho crítico. Ver Architecture Review no `epic.md`.
- Espelhar a estrutura de `approve_content_request` (`app/api/content.py:420-455`) para auth/ownership/404, trocando o efeito final: em vez de `approved` + `publish_post.delay`, seta `scheduled` + `scheduled_for`.
- Schema Pydantic novo para o body (ex.: `ScheduleRequest { scheduled_for: datetime }`) em `app/schemas/content.py`.
- **@data-engineer:** a migração deve adicionar o valor ao enum Postgres (`ALTER TYPE ... ADD VALUE 'scheduled'` — atenção: não pode rodar dentro de transação em algumas versões; seguir o padrão das migrações de enum já existentes no projeto) e criar o índice `(status, scheduled_for)`.
- `scheduled_for` sempre timezone-aware; o Celery já opera em `America/Sao_Paulo` (`app/tasks/__init__.py`).

## Tasks
- [x] Adicionar `scheduled = "scheduled"` ao enum `ContentStatus`
- [x] Adicionar coluna `scheduled_for` (DateTime timezone-aware) ao modelo
- [x] Migração Alembic: coluna + valor de enum + índice `(status, scheduled_for)`
- [x] Schema Pydantic `ScheduleRequest`/`ScheduleResponse` para o body/resposta
- [x] Endpoint `POST /content-requests/{id}/schedule` com validações (futuro, ≤30d, status, ownership)
- [x] Testes cobrindo AC7
- [x] Confirmar `/approve` inalterado (diff limpo — só adição, nenhuma linha do endpoint tocada)

## Change Log
| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-07-25 | 1.0 | Story criada a partir do Epic 19 + Architecture Review | @sm (River) |
| 2026-07-25 | 1.1 | Validação PO: GO (9/10). Estimativa adicionada. Status Draft → Ready. | @po (Pax) |
| 2026-07-25 | 1.2 | Implementação completa: enum, coluna, migração, endpoint, testes. Suite completa 368/368. Status Ready → Ready for Review. | @dev (Dex) |
| 2026-07-25 | 1.3 | Gate PASS. Push para produção (Railway aplicou a migração automaticamente via releaseCommand). /health confirmado ok após deploy. Status Ready for Review → Done. | @devops (Gage) |

## Dev Agent Record

### Agent Model Used
claude-sonnet-5 (@dev — Dex)

### Debug Log References
- `pytest tests/test_schedule_api.py -v` → 7/7 passed
- `pytest tests/test_approval_api.py -v` → 9/9 passed (confirma `/approve` intacto)
- `pytest tests/ -q` (suite completa) → 368 passed, 0 failed, ~184s

### Completion Notes List
- Enum `ContentStatus.scheduled` adicionado entre `approved` e `publishing`, conforme desenhado no epic.
- Coluna `scheduled_for` (timezone-aware) adicionada ao modelo `ContentRequest`.
- Migração `a1b2c3d4e5f6` (down_revision `f9a0b1c2d3e4`, head atual do projeto) segue o padrão já usado em `e2f3a4b5c6d7` (ALTER TYPE ADD VALUE isolado) e `f9a0b1c2d3e4` (add_column com comment) — coluna + valor de enum + índice composto `(status, scheduled_for)` numa única migração, conforme AC3. **Não apliquei a migração contra o banco real** (Supabase de produção) — isso é uma ação com efeito em produção que requer aprovação explícita fora do escopo de implementação; fica para o momento do deploy/push.
- Endpoint `POST /content-requests/{id}/schedule` inserido logo após `/approve` em `app/api/content.py`, replicando exatamente o padrão de auth/ownership/status (404 → 403 → 409) e adicionando as validações de janela temporal (422 passado, 422 >30 dias). Normaliza `scheduled_for` sem timezone para UTC antes de comparar.
- **Decisão durante a implementação:** inicialmente escrevi também o endpoint de cancelar (`/schedule/cancel`), mas revertido — cancelar/reagendar é escopo explícito da Story 19.2 (AC4/AC5 de lá). Removido para manter o gate desta story fiel aos ACs 19.1; comentário no código aponta para onde será implementado.
- `/approve` (`app/api/content.py:420-460`) não sofreu nenhuma alteração de linha — confirmado por diff e pelos 9/9 testes de `test_approval_api.py` passando sem modificação.
- Regressão completa: 368/368 testes passando, 0 falhas — nenhuma quebra em nenhum outro módulo.

### File List
**Backend:**
- `backend/app/models/content_request.py` (modificado) — enum `scheduled` + coluna `scheduled_for`
- `backend/migrations/versions/a1b2c3d4e5f6_add_scheduling_to_content_requests.py` (novo) — migração
- `backend/app/schemas/content.py` (modificado) — `ScheduleRequest`, `ScheduleResponse`
- `backend/app/api/content.py` (modificado) — endpoint `POST /{id}/schedule`
- `backend/tests/test_schedule_api.py` (novo) — 7 testes cobrindo AC7

## QA Results

### Review Date: 2026-07-25

### Reviewed By: Quinn (Test Architect)

### Verificação de Acceptance Criteria (leitura direta do código)

- **AC1/AC2** (enum `scheduled` + coluna `scheduled_for`) — confirmado em `app/models/content_request.py:30,72-75`.
- **AC3** (migração) — confirmado em `migrations/versions/a1b2c3d4e5f6_...py`: coluna + valor de enum + índice `(status, scheduled_for)` numa única migração. O `ALTER TYPE ADD VALUE` está isolado de qualquer uso do novo valor na mesma transação — sem risco do bug clássico de Postgres (usar valor de enum recém-adicionado na mesma transação que o criou). Segue exatamente o padrão já validado em `e2f3a4b5c6d7`.
- **AC4** (`POST /{id}/schedule`) — confirmado em `app/api/content.py:470-518`: seta `scheduled` + `scheduled_for`, não chama `publish_post`.
- **AC5** (validações) — confirmado: 422 passado/>30 dias, 409 status errado, 403 outro cliente, 404 inexistente.
- **AC6** (`/approve` intocado) — confirmado por `git diff` (zero linhas alteradas no endpoint) e pelos 9/9 testes originais de `test_approval_api.py` passando sem nenhuma modificação.
- **AC7** (testes) — `test_schedule_api.py` cobre os 6 cenários pedidos + bônus de 404. 7/7 passando.

### Testes executados nesta revisão (independente do relato do @dev)

- `pytest tests/test_schedule_api.py -v` → **7/7 passed**
- `pytest tests/test_approval_api.py -v` → **9/9 passed** (confirma AC6)
- `pytest tests/ -q` (suíte completa) → **368 passed, 0 failed**

### Achados

**FUTURE-001** (baixo, não bloqueante): `scheduled_for` não está exposto em `ContentRequestDetailResponse` — não é gap desta story, mas a Story 19.5 (lista de agendados) vai precisar disso no GET. Recomendado adicionar antes/durante a 19.5.

**INFO-001** (baixo, informativo): normalização de datetime naive para UTC é defensiva e correta, mas sem teste dedicado. Não bloqueante.

### Segurança
Ownership/existência/status replicam exatamente o padrão já auditado do `/approve` — sem superfície nova de risco.

### Gate Status

Gate: **PASS** → `docs/qa/gates/19.1-agendamento-modelo-endpoint.yml`

Motivo: todos os 7 ACs confirmados por leitura de código independente, testes próprios rodados (não só confiança no relato), regressão completa limpa. Único ponto é uma recomendação para a Story 19.5, não um problema desta story.
